/**
 * Add support for parsing from HTML with DOM APIs.
 *
 * @param {Readonly<Options> | null | undefined} [options]
 *   Configuration (optional).
 * @returns {undefined}
 *   Nothing.
 */
export default function parse(options?: Readonly<Options> | null | undefined): undefined;
export default class parse {
    /**
     * Add support for parsing from HTML with DOM APIs.
     *
     * @param {Readonly<Options> | null | undefined} [options]
     *   Configuration (optional).
     * @returns {undefined}
     *   Nothing.
     */
    constructor(options?: Readonly<Options> | null | undefined);
    parser: (value: string) => Root;
}
import type { Options } from 'rehype-dom-parse';
import type { Root } from 'hast';
//# sourceMappingURL=index.d.ts.map